<!--
  integrations.md — the project's tool/integration map. Generated by /sdd:discover-tools
  (or the setup-sdd skill) following .sdd/settings/rules/tooling-discovery.md.
  Loaded as project memory; read by /prepare-pr, /review, /data-quality before they act.
  Keep it factual and current. Re-run /sdd:discover-tools when tooling changes.
-->

# Integrations & Available Tools

_Last discovered: {{DATE}}_

## Capability map

| Area | Backend | Access method | Tool / command | Note |
|------|---------|---------------|----------------|------|
| Issue tracking | {{Jira \| Microsoft Planner \| Kanbanize \| Linear \| GitHub Issues \| none}} | {{mcp-native \| cli \| needs-setup \| n/a}} | {{e.g. Atlassian MCP}} | {{e.g. project key / board id}} |
| VCS / PR host | {{GitHub \| GitLab \| Azure DevOps \| Bitbucket \| none}} | {{...}} | {{e.g. `gh`}} | {{remote: git@host:org/repo}} |
| Data platform | {{Databricks \| BigQuery \| Snowflake \| none}} | {{...}} | {{e.g. `databricks` / `bq`}} | {{project/dataset/catalog}} |
| CI/CD | {{GitHub Actions \| Azure Pipelines \| GitLab CI \| Bitbucket Pipelines}} | {{...}} | {{...}} | {{where checks run}} |

_Declared in setup: VCS = {{user answer}} · tracker = {{user answer}}. Flag any mismatch with what was detected._

**Access method legend**
- `mcp-native` — an MCP server is connected; prefer it (structured, safer).
- `cli` — an authenticated CLI is installed and usable.
- `needs-setup` — nothing usable found; must be configured before dependent commands work.
- `n/a` — not relevant to this project.

## To enable (open setup items)

<!-- One line per `needs-setup` row: what to add. Delete if none. -->
- {{area}}: configure {{MCP server name or CLI + auth}} — {{one-line pointer}}.

## How commands use this

- `/prepare-pr` and `/review` → **VCS / PR host** row.
- `/data-quality` → **Data platform** row.
- Spec ↔ ticket linking → **Issue tracking** row.

If a command's required row is `needs-setup`, the command must stop and tell the user what to
configure — it must not guess at a different tool.
